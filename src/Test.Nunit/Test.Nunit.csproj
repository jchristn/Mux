<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <Version>0.5.0</Version>
    <Description>NUnit runner for MUX Touchstone suites</Description>
    <ImplicitUsings>disable</ImplicitUsings>
    <IsPackable>false</IsPackable>
    <IsTestProject>true</IsTestProject>
    <!--
      Run the per-TFM test hosts (net8.0, net10.0) sequentially rather than in parallel.
      These suites spin up loopback HttpListener mock servers; running both TFM hosts at
      once doubles loopback/port contention on CI runners and pushes transient connection
      failures past the client's retry budget, flaking network-dependent cases such as the
      probe contract test. The console runner (Test.Automated) already runs the same suites
      one TFM at a time and stays reliable — this mirrors that behavior for `dotnet test`.
    -->
    <TestTfmsInParallel>false</TestTfmsInParallel>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="coverlet.collector" Version="10.0.1" />
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="18.9.0" />
    <PackageReference Include="NUnit" Version="4.6.1" />
    <PackageReference Include="NUnit.Analyzers" Version="4.14.0" />
    <PackageReference Include="NUnit3TestAdapter" Version="6.3.0" />
    <PackageReference Include="Touchstone.NunitAdapter" Version="0.1.12" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Test.Shared\Test.Shared.csproj" />
  </ItemGroup>

</Project>
